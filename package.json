{
  "name": "al80-studio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "WebHID control panel for the YUNZII AL80 — LCD (clock, picture, GIF, slideshow), RGB lighting, and a live VIA keymap editor.",
  "scripts": { "test": "node test/protocol.test.mjs && node test/keymap.test.mjs" }
}
